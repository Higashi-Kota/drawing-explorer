# Drawing Explorer

ブラウザのOPFS (Origin Private File System) にドローイングを保存できるアプリ。ドッキングパネルとファイルエクスプローラーUI付き。

## Quick Start

```bash
pnpm install
pnpm --filter @internal/design-tokens build
pnpm dev:frontend
```

開発サーバー: http://localhost:5000
